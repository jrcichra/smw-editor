# Lunar Magic Feature Parity Tracker

Tracks what Lunar Magic (LM) can do vs. what smw-editor currently supports, so
work can be prioritized toward feature completeness. Update this file as
features land — flip status and add the PR/commit that did it.

Status legend: ✅ Done · 🟡 Partial · ⛔ Missing

**Confidence note:** this was built from one codebase-survey pass plus
targeted greps, not an exhaustive audit. Two follow-up checks already
corrected the first draft (sprite extra-bits were wrongly marked missing;
freespace-finding was undersold as overworld-only). Treat ⛔ rows as "not
found by grep," not proven absent — re-verify with a grep/read before relying
on a row for planning if it's been a while since the file was touched. Missing
areas not yet cross-checked in depth: layer 3 "tide"/water settings across
levels, direct Map16 import/export file format, and player (Mario/Yoshi)
graphics customization. (ROM search/analysis is now covered: PR #4 added ROM-wide cross-reference
search — "find all references" for sprites, objects, tiles, music, and exits.)

## Level Editing

| Feature | Status | Notes |
|---|---|---|
| Object placement/move/resize (layer 1) | ✅ | `level_editor/object_layer.rs`, `editing.rs` |
| Sprite placement | ✅ | `sprite_layer.rs`, `sprite_catalog.rs`; global per-ID behavior editable via `sprite_tweaker_editor.rs` (see Sprites section) |
| Main-entrance editing in sprite mode | ✅ | Lunar Magic v2.20: the red "M" marker is selectable in sprite editing mode (click; orange selection outline like sprites, exclusive with sprite selection), plain-draggable without Shift (one undo step per drag), and participates in the clipboard — Ctrl+C copies its position as `smwclip:1:entrance:x,y` text, Ctrl+X cuts, Ctrl+V pastes = moves the entrance to the cursor (cascades otherwise), Delete/Backspace and the Erase tool reset it to the vanilla default tile (0, 22) (half-res (0,11) screen 0 — verified as Nintendo's factory default: 374 of 512 real-ROM levels carry it). The position is its own `UndoableData<SpawnPos>` with cross-stack undo priority, so Ctrl+Z/Ctrl+Y work on entrance edits in any mode (a rare entrance/sprite/entrance interleave can swap two undos' order; every step stays undoable). Honest limits: LM's "dragging the entrance auto-updates the FG/BG initial position" semantics need verification against real Lunar Magic before implementing (not attempted here). See `docs/screenshots/entrance-edit.gif` |
| Primary/secondary header editing | ✅ | `properties.rs`, `left_panel.rs` — raw byte fields. **LM v3.00 entrance extras:** new "Entrance Extras" section in the Level Header window — Face Left, New FG/BG Init System, BG Relative to FG Only (enabled only with the new init system), and BG Height (hex) — for the main entrance, persisted in the editor-owned `SMWENTR1` RATS block (`crates/smwe-rom/src/level/entrance_extras.rs`; merged on save, untouched ROMs stay byte-identical). **LM v2.20:** Alt+Right-click the red `M` entrance marker in sprite mode opens the Level Header (Edit Manual takes precedence when the click also hits an object/sprite); Shift+dragging the marker moves the entrance and the secondary-header bytes are rewritten on save. Honest limits: in-game playback of all v3.00 options needs Lunar Magic's ASM hacks, which this editor does not install; entrance copy/delete in sprite mode is not implemented yet. Verified: `cargo test -p smwe-rom` (RATS codec round-trips incl. scratch-ROM write/parse + real-ROM ignored tests). See `docs/screenshots/entrance-extras.png` |
| Dynamic level dimensions — horizontal level height (LM v3.00) | 🟡 | Per-level custom horizontal height beyond the vanilla 27 tiles, within LM's tilemap-RAM budget (screens × height ≤ 896 — verified byte-exact against LM's 32-entry height LUT recovered from `LmLevelEntry.Height.cs`: 32 screens → 27, 6 → 149, 1 → 896). Model: `LevelHeights` in `crates/smwe-rom/src/level/dimensions.rs`, persisted in a native RATS block (`SMWLVLH1`) and parsed into `SmwRom.level_heights`; MWL export/import carries the height in reserved level-info bytes 6–7 (0 = vanilla, backward compatible). UI: "Level Height" slider in the Level Header window (max derived from the screen-count budget, clamped live when Level Length changes) and the canvas renders the full custom height; object/sprite placement and drags clamp to the vanilla-encodable rows 0–31 (LM's 32-row band objects, ext 01/03, not implemented). Honest boundary: no in-game engine is installed — without LM's dynamic-dimensions ASM hooks a ROM still plays 27 tiles; the slider says so. Unit + real-ROM ignored tests. See `docs/screenshots/level-height-dialog.png` and `docs/screenshots/level-height-canvas.png` |
| Screen exits / secondary entrances | 🟡 | `secondary_entrance_editor.rs` + `crates/smwe-rom/src/level/secondary_entrance.rs`: vanilla 0x200-entry table editing (grid of dest level/screen/X/Y/FG/BG + jump-to-level, unchanged); LM v2.50 parity — the index combo accepts typed full values 0x000–0x1FFF and the editor stores vanilla-format entries at indices 0x200–0x1FFF; LM v3.00 parity — per-entrance water-level flag, exit-to-overworld (normal/secret exit, player switch, base event, teleport location), and midway-entrance redirect to another level, plus an overworld toolbar "Secondary Exit Teleports…" button editing the 0x100-entry Star/Pipe teleport table (`world_editor/se_teleport_editor.rs`). All LM-added data lives in one editor-owned RATS block (`SMWESEX2`), merged on save between the level and overworld tabs so neither clobbers the other; untouched ROMs stay byte-identical. Vanilla layout verified against SMWDisX `bank_05.asm` `CODE_05D796`/`DATA_05F800` (the `UseSecondaryExit` branch reads the four byte-lanes `$05:F800`/`$05:FA00`/`$05:FC00`/`$05:FE00`); the Star/Pipe table has no vanilla counterpart (nothing matching in SMWDisX). Honest limits: in-game playback of the expanded table and all v3.00 options needs Lunar Magic's ASM hacks, which this editor does not install. Verified: `cargo test -p smwe-rom` (RATS codec round-trips incl. corrupt/foreign-block rejection) + real-ROM ignored tests (vanilla ROM parses with no ext block; write→re-parse on an in-memory copy leaves the vanilla table byte-identical). See `docs/screenshots/secondary-entrances-expanded.png` and `docs/screenshots/ow-teleport-table.png`. **LM v3.00 entrance extras:** per-secondary-entrance Face Left and New FG/BG Init System checkboxes in the extended-options panel (badges `FL`/`FG`); the `SMWESEX2` payload is now v2 (v1 payloads still decode with the new flags defaulted off). See `docs/screenshots/secondary-exit-v300-extras.png` |
| Map16 tile picker & editor | ✅ | `tile_picker.rs`, `map16_editor.rs` |
| Direct Map16 access (+ Conditional, Remap) | ✅ | `crates/smwe-rom/src/direct_map16.rs` + `level_editor/dm16_editor.rs` (LM v1.70–v1.90 parity): "Add Objects / Direct Map16" window — drag a rectangular Map16 pattern from the 512-block picker, arm placement, click the level to drop it as one distinguishable (purple overlay) resizable object; the pattern repeats on resize (inspector W/H sliders, one undo step per action). Conditional Direct Map16 stores a per-object RAM flag (address + bit 0–7 / 8 = nonzero); Remap rewrites old→new tile IDs across the level's DM16 objects. Ctrl+Shift+RightClick flood-fills an enclosed blank region with the current pattern (one undo step, merged into row-run objects). Map16 clipboard payloads paste into the level as DM16 objects. Objects are undoable (`EditableDirectMap16`), stamped into the WRAM block map on load and after every edit/undo/redo (both level orientations), saved to the editor-native `SMWDM161` RATS block (standard RATS size semantics, like `exanimation.rs`) and carried on MWL section 8 (editor-native ninth section; real LM 8-section files still decode). **Honest limits:** the condition is inert metadata on a stock ROM (needs an ASM patch to evaluate); in-game rendering needs LM's Direct Map16 ASM (not installed by this editor). See `docs/screenshots/direct-map16.png` |
| Palette editor (BG/FG/sprite) | ✅ | `palette_editor.rs`: click-to-select swatches, sRGBA color picker, per-group ROM palette index. **Undo/redo (LM v1.80)**: the three 12-color groups live in `UndoableData<EditablePalettes>` (72-byte LE u16 blob); Undo/Redo buttons at the top of the window + Ctrl+Z/Ctrl+Y while the window is hovered (consumed before the level-canvas shortcuts). A color-picker drag commits one undo step on pointer release, so undo restores the pre-drag color instead of walking back through every drag frame. Undo marks the palette dirty so Ctrl+S writes it. The Map16 Block Editor (`map16_editor.rs`) has the same treatment (**LM v1.91**): per-block tile-word edits in `UndoableData<EditableMap16Edits>` (sorted `BTreeMap` serialization, so undo deltas are deterministic); Undo/Redo buttons + Ctrl+Z/Ctrl+Y; slider drags commit one undo step on release; paste and "Revert to ROM" are single undo steps. **RGB hex entry (LM v3.50)**: an "RGB #" field beside the picker accepts `RRGGBB` (optional leading `#`); Enter/Apply is one undo step (`parse_hex_rgb`, `apply_palette_hex`, `snes_to_hex_rgb` in `palette_editor.rs`), invalid input is rejected with an "invalid hex" hint, and a mid-drag apply commits the in-flight picker gesture first so the edits stay separate. See `docs/screenshots/palette-hex-entry.png`. **Custom palette (LM v3.30)**: "Enable custom palette" gives the level its own private BG/FG/sprite palettes instead of the shared tables, so edits stop recoloring every other level that shares the table entry; "Auto-enable custom palette on edit" (LM v3.30 option, on by default) enables it automatically on the first palette edit. Palettes live in one editor-owned RATS block (`SMWECPLT`, `crates/smwe-rom/src/level/custom_palette.rs`), merged per-level on save so other tabs' entries are never clobbered; disabling erases the level's entry and returns to the shared tables. A ROM with no custom palettes has no block and stays byte-identical. See `docs/screenshots/palette-custom.png`. See `docs/screenshots/palette-map16-undo.png`. **Palette file buttons (LM v1.40 / v1.80)**: the Palette Editor has Extract/Insert Shared Palette and Export/Import Custom Palette (.mw3) buttons (File-menu parity, `palette_editor.rs::palette_files_section` + `src/palette_files.rs`): shared-palette files are 576 bytes (3 groups × 8 rows × 12 LE u16, byte-identical to the ROM ranges `$00B0B0`/`$00B190`/`$00B318`); `.mw3` files are the 514-byte / 257-word format (v1.40 changelog: "export and import custom level palette files (MW3)", FuSoYa 2001-12-29) carrying this level's 36 colors in words 0..36 with words 36..257 zeroed on export and ignored on import. Insert/import are failure-atomic (wrong size is rejected without touching the palette) and each is one undo step (the undo record now holds the 648-byte palette state: 72 on-screen + 576 shared); `.mw3` import auto-enables the custom palette (LM v3.30 semantics) so the import lands in the level's private palette, never the shared tables. Palette edits in non-custom mode write to the shared tables, and the save path merges only rows dirtied by the saving tab (merge-on-save), so multi-tab shared-palette edits no longer clobber each other. See `docs/screenshots/palette-file-buttons.png`. |
| Layer 2/3 background editing | 🟡 | `background_layer.rs` minimal (~510 bytes); the 5-byte Layer 2 object-data header is now user-editable in the Level Header window (hex byte sliders) and written back on save instead of copied verbatim. The game skips these bytes (SMWDisX `bank_05.asm`: +5 "to ignore Layer 2's header"), so edits only change the stored bytes; in the vanilla ROM they mirror the level's primary header. Model: `Layer2Data::Objects { header, objects }` + `LAYER2_HEADER_SIZE` (`crates/smwe-rom/src/level/mod.rs`); mwl export now carries the model's header. Verified by real-ROM ignored tests (parsed header == pointer bytes for every L2-objects level; edit → write → re-parse round-trip with the object stream byte-identical). See `docs/screenshots/l2-header-editing.png` |
| Background Tile Map Editor (WYSIWYG) | ✅ | Dedicated Lunar Magic-style Background Tile Map Editor window (`level_editor/bg_tilemap_editor.rs`, launched from the PANORAMA toolbar on background levels): true 32x27 WYSIWYG canvas rendered from the real BG Map16 table + VRAM/CGRAM (shared `render_util::render_bg_tilemap` also used by the headless screenshot), palette-colored 16x16 tile selector, 1x/2x zoom, tile grid toggle, paint/select/eyedropper, drag-preview with one-undo-step commits, undo/redo, selection resize as repeating pattern, Select All, right-click erase, Shift+right-click pattern flood fill, middle-click pick, and the four LM bank/tile operations (Change Background Map16 Bank, Remap Background Tiles, Copy Background Image, Add Offset to Background Tiles/F9 with automatic bank switching via the pure `smwe_rom::level::background::bg_tile_offset` helper). Model: `BG_TILEMAP_*` layout constants, `bg_cell_index`/`bg_cell_pos` two-half mapping, `bg_high_byte_for_pointer` ($0CE8FE boundary, confirmed in SMWDisX `bank_05.asm` `CODE_058126`), bank-aware `write_background_to_rom` (reuses the old location when the page is unchanged and data fits, otherwise relocates strictly below/at-above $E8FE or fails with an explicit no-space error — never writes the wrong side). Verified: `cargo test -p smwe-rom` (incl. new offset/bank/layout unit tests) plus real-ROM ignored tests (edit→write→re-parse round-trip; bank-change lands on the correct side or fails cleanly). See `docs/screenshots/bg-tilemap.png` |
| Layer 3 "Change Layer 3 Settings" dialog + GFX bypass | 🟡 | "Layer 3 Settings\u2026" button in the Level Header window opens the "Change Layer 3 Settings" dialog (`level_editor/layer3_settings.rs`): named 0-3 setting selector decoded from the vanilla `Layer3TilemapSettings` ($00:9F88) + `Layer3Ptr` ($05:9000) tables (tide/rising vs stationary, tileset-specific scroll names, cage/crusher/windows/rocks/clouds/fish), Layer 3 priority checkbox (primary header bit 7), and a WYSIWYG stripe preview rendered from the resolved vanilla stripe image through the emulator's VRAM/CGRAM. Per-level GFX bypass (`smwe_rom::layer3::Layer3GfxBypass`, RATS `L3BP` block, persisted on save) changes the preview; applying it in-game on a vanilla ROM needs Lunar Magic's closed-source "Layer 3 GFX and tilemap bypass" ASM hack — the dialog says so explicitly. Model: `crates/smwe-rom/src/layer3.rs` (stripe RLE decode, 64\u00D764 tilemap apply, named kinds). Verified by real-ROM ignored tests (all 7 vanilla stripe pointers resolve and decode; every tileset\u00D7setting combination resolves). See `docs/screenshots/layer3-settings.png` |
| Music selection | ✅ | Named vanilla track picker (`crates/smwe-rom/src/music.rs`, derived from SMWDisX `LevelMusicTable` in `bank_05.asm`): all 8 tracks with raw-value fallback for remapped ROMs; xref search also shows track names |
| Custom level names (overworld name table) | 🟡 | Beyond Lunar Magic (LM has no level-name editing): implemented — see "Custom level names" in Overworld Editing below. All 93 vanilla names decode from the real ROM; encode→patch→decode round-trip verified |
| Message box / dialog text editor | ✅ | `crates/smwe-rom/src/message_boxes.rs` + `level_editor/message_editor.rs` — all 22 vanilla messages, verified against real ROM (exact byte boundaries derived from `symbols/SMW_U.sym` label addresses; total size matches the ROM's already-100%-utilized 2854-byte budget exactly). PR #5 added a read-only WYSIWYG preview: true GFX2A message font rasterization (SNES $0BCB7B, 2bpp, 128 tiles) plus a `CODE_05B1BC` stripe capture, shown as an 8×18 raster per message, validated against the real ROM. Phase 2 (this PR) made the text itself editable: a multiline field shows decoded text (`\n` = line break; the real routine has no other control codes) and typing re-encodes to tile bytes live, with per-message byte-budget enforcement (`used / budget`, over-budget input refused, not truncated). Non-text graphic tiles (Yoshi's signature, bonus-star icons) show as `�` and are preserved in place. Decode→encode round-trips all 22 vanilla messages byte-exactly |
| Import/export level as `.mwl` | 🟡 | `crates/smwe-rom/src/mwl.rs` + `level_editor/mwl.rs`: toolbar Export/Import buttons. Binary MWL v3.63 container (header, 9-entry directory, 9 sections: the 8 real LM sections plus the editor-native ninth); level-info (level number + secondary header), Layer 1 (5-byte primary header + objects), Layer 2 objects or legacy background (0x800 LE words; vanilla mapping verified: entries 0x000–0x1AF→words 0x000–0x1AF, 0x1B0–0x35F→0x200–0x3AF; descriptor $08/$18 from LM 3.63's own binary; high byte from the game's $E8FE boundary, confirmed in SMWDisX `bank_05.asm`), sprites (1-byte header + stream). Export applies unsaved edits via scratch ROM; import repoints into free space, writes the ROM atomically (backup + rename), and reloads. Validated on the real ROM: export shape of level 0x105, export→import→re-export payload-identical round-trips for a background level (0x0) and an L2-objects level (0x9). v1 limits: vanilla layouts only (no RATS/LC_LZ2/3); palette and secondary entrances sections export empty and are rejected on import when non-empty. ExAnimation (section 6) round-trips the level's animation list. ExGFX/bypass (section 7) is supported: export writes file references + per-level bypass overrides in LM's `nnnExGFX.bin`/`nnnnnnnn` section-7 format, and import inserts the referenced files and re-homes bypass records to the target level. Direct Map16 objects round-trip on editor-native section 8 (empty on levels without DM16 objects; ignored on import when empty). See `docs/screenshots/mwl-import-export.png` |
| Move/resize via drag handles (LM-style) | ✅ | `level_editor/editing.rs` (`DragHandle`, `ObjectDrag`, `update_object_drag`) + overlay in `central_panel.rs`: selecting a single layer-1 object shows 8 LM-style handles (4 corners + 4 edge midpoints, white squares with black borders) plus a yellow selection outline. Dragging the body moves the object; dragging a handle resizes it (settings byte: low nibble = width−1, high nibble = height−1, clamped 1–16 tiles; drag edge follows the pointer, opposite edge fixed). Live transient feedback during the drag; release commits exactly one undoable write (`obj.x/y` + settings) and moves the tile-map footprint (old area blanked with 0x25, blocks re-stamped with edge-stretch for grown cells). Extended (1×1) objects move but don't resize (their settings byte is the object ID). Hover cursors per handle direction, grab hand on body; object drags suppress canvas panning and release-click re-selection. Unit-tested geometry incl. settings round-trip over all 1–16×1–16 sizes and the W/N far-drag 16-tile clamp regression test. See `docs/screenshots/drag-handles.png` and the animated `docs/screenshots/drag-handles.gif` |
| Editable ExAnimation (per-level + global) | 🟡 | `crates/smwe-rom/src/exanimation.rs` + `src/ui/exanimation_dialog.rs` shared "ExAnimated Frames" window (level editor: toolbar film-strip button; world-map editor: "ExAnimated Frames…" button — the overworld list, LM v2.40 parity): line (8x8/16x16), palette, and palette-rotate frame types with destination, speed, trigger (Always/On-Off/Manual/One-shot), step count, and units-per-step; add/delete/reorder; VRAM tile-browser source assignment for line frames; color pickers for palette frames; disable-original-animation flag. Per-level list lives on MWL section 6 (export/import round-trips); per-level + global + overworld lists stored natively in a RATS-tagged free-space block (`SMWEXAN1`, format v2; v1 blocks decode with an empty overworld list). Live preview: the editors tick every ~133ms, apply VRAM tile copies and CGRAM updates, and can suppress the vanilla animation. Non-Always triggers are stored but previewed as Always. Honest limits: this authors and previews the animation data — in-game playback needs Lunar Magic's ExAnimation ASM (not installed by this editor). Verified by unit + real-ROM ignored tests. See `docs/screenshots/exanimation-preview.png`, the animated `docs/screenshots/exanimation-preview.gif`, and the overworld `docs/screenshots/ow-exanimation-preview.gif` |
| Export level(s) to PNG image | ✅ | LM v2.30 ("Export Level to PNG") + v3.20 ("Export Multiple Levels to Image Files" under File > Levels): `src/level_png_export.rs` shared render pipeline drives the real emulator (`decompress_sublevel` + the composed-VRAM tilemap path from `render_util`), backdrop fill, Layer 2 → Layer 1 → sprite pass, with per-layer toggles — used by both the File-menu actions and the headless `render_level` binary so the two can never drift. File > "Export Level to PNG..." exports the focused level-editor tab (falls back to the first open level editor) via a save dialog defaulting to `level_XXX.png`; File > Levels > "Export Multiple Levels to Image Files..." opens a batch dialog (hex range 000–1FF, Layer 1/Layer 2/Sprites toggles, output-folder picker) that writes one PNG per level (`level_000.png` … `level_1FF.png`). Both merge unsaved tab edits into the rendered ROM first (same merge the BPS/IPS exports do). Verified: unit tests (hex-padded filename format, `LEVEL_COUNT == 0x200`) + real-ROM ignored tests (level 0x105 renders exactly 8192×432, contains non-backdrop pixels, PNG encodes/decodes round-trip; levels 0x000/0x100/0x1FF also render). See `docs/screenshots/export-levels-png.png` (dialog mock + real export thumbnails) |
| Delete Levels from ROM | ✅ | LM v3.50 (File > Levels > "Delete Levels from ROM..."): `crates/smwe-rom/src/level_deletion.rs` repoints each selected level's Layer 1 ($05E000 table), sprite ($05EC00), and Layer 2 ($05E600) pointers at the vanilla test level ($068000 / $07E76D / $FFD900 — the targets 277/278/283 vanilla slots already share, verified via SMWDisX labels `TestLevel`/`TestLevelSprites`/`DATA_0CD900` and a real-ROM pointer-table scan), then erases every displaced data block no remaining level references (0xFF-fill, reclaimed as free space; blocks shared with other levels, e.g. the Mode-7 boss rooms, and bank-$FF background data are never erased) and repairs the ROM checksum/complement pair. File > Levels > "Delete Levels from ROM..." dialog: All/Modified/Unmodified/None quick-select (modified = pointer-table entry or block bytes differ vs the ROM-as-opened reference copy), red gameplay-critical warning for the title/demo levels (0C5/0C7/0C8) and overworld-placed levels, confirmation dialog, automatic restore point, atomic install with .bak. Verified by unit + real-ROM ignored tests. See `docs/screenshots/delete-levels.png` |
| Edit Manual — raw object/sprite byte editing | ✅ | LM v1.91 (Edit menu → "Edit Manual", also Alt+Right-click): manually modify the selected object/sprite at the byte level, incl. extension fields of multibyte objects/sprites (v1.80). `src/edit_manual.rs` shared byte codec (standard/extended object classification, sprite entries, LM-style hex parsing, decoded-field summaries) + `level_editor/edit_manual_dialog.rs` dialog: toolbar ✏️ button (enabled for exactly one selected object/sprite) or Alt+Right-click on an object/sprite opens it; three hex fields with a live "Decodes as:" readout; Apply writes the bytes back in one undoable write (object → x/y/id/settings via the entry's screen; sprite → x/y/id/extra bits), honoring Ctrl+Z/Ctrl+Y. Refusals: exit and screen-jump byte patterns (exits stay in the Secondary Entrances editor), standard↔extended kind changes, unparseable hex. The new-screen flag (bit 7 of byte 0) is shown but recomputed at save time. Unit-tested codec incl. byte-exact cross-checks against `smwe_rom::objects::Object` accessors; `stream_bytes_for_index` round-trips against the serializers. See `docs/screenshots/edit-manual.png` |
| Custom Collections of Objects (Add Objects category) | ✅ | LM v3.60 (re-added the "Custom Collections of Objects" category to the "Add Objects" window — it briefly appeared in LM 1.00; per-user named groups of custom extended objects, useful for the 3-byte extended-object definitions that control various level settings). `src/custom_collections.rs` shared model (per-user JSON at the platform config dir — `%APPDATA%/smwe`, `~/Library/Application Support/smwe`, or `$XDG_CONFIG_HOME/smwe` — not ROM data; add/rename/delete collections and entries, hex ID field, reserved IDs 0x00/0x01 refused since they would decode as exit / screen jump on reload) + `level_editor/custom_collections_ui.rs`: toolbar toggle opens the manager window, and a "Custom Collections of Objects (LM 3.60)" picker in the Draw-mode left panel arms an entry so the next canvas click places that extended object (`is_extended`, `extended_id` = entry ID) instead of painting tiles — placed entries show in the object overlay, are selectable/erasable, and serialize through the normal object stream. Unit-tested (JSON round-trip incl. corrupt-file tolerance, ID parsing, reserved-ID refusal). See `docs/screenshots/custom-collections.png` |
| "Mark exit-enabled tiles" view option | ✅ | LM v3.31 (View menu): translucent green markers over Layer 1 tiles whose act-as root is exit-enabled — doors 0x01F/0x020/0x027/0x028, exit-enabled pipes 0x137/0x138/0x13F, plus 0x09C in level mode 0x01 only; the object-backed Layer 2 is also evaluated in mode 0x01, matching the game's interactive layer. Predicate in `crates/smwe-rom/src/block_behavior.rs::is_exit_enabled` (unit-tested); overlay in `central_panel.rs` reads the WRAM block maps live so markers track unsaved object edits and staged acts-like changes; toolbar toggle (🚪 icon) + F9. Honest limits: no `.dsc` parser, so `.dsc`-flag bit 8 (which LM treats as additive) is not modeled. See `docs/screenshots/exit-enabled-tiles.png` |
| Scan for Undefined Exits | ✅ | LM v1.50 (menu scan for exit-enabled objects leading to the bonus-game levels) + v1.60 (toolbar "Scan for Undefined Exits" button): `crates/smwe-rom/src/exit_scan.rs` resolves each level's 4-byte screen-exit objects (direct destinations and LM v3.00 secondary exits, skipping exit-to-overworld secondaries) and `src/exit_scan.rs::scan_undefined_exits` walks all 512 levels through the real emulator — decompressing each level and reusing the exit-enabled-tiles block-map geometry (`src/level_png_export.rs`) to find every screen holding exit-enabled tiles, then checking its screen-exit record. Flags screens with no exit record, and exits resolving to the TEST levels $000/$100. Tools > "Scan for Undefined Exits..." runs it on a worker thread (the full scan takes ~2 min through the emulator) over ROM bytes with unsaved tab edits merged, with a progress bar and a scrollable results window listing level/screen/reason. Verified: unit tests + real-ROM ignored tests; vanilla ROM scan finds 19 genuine issues (unused levels $1BB–$1F8 with unconfigured secondary exits, plus Layer-2 $09C exit tiles on screens 7/9 of $11D/$1E8/$1E9 with no screen exit). See `docs/screenshots/exit-scan.png` |
| Analyze Resources in Levels | ✅ | LM v3.03 (File > Levels menu: "check which Map16 tiles, GFX/ExGFX files, and sprites are used in which levels then generate a text file report"; music-track reporting added in LM v3.20): `src/resource_scan.rs` walks all 512 levels (unsaved tab edits merged first) and summarizes each level — header music track (named via `smwe-rom::music`), placed sprite IDs, distinct Map16 tile IDs from the Layer 1 block map (decompressed through the real emulator, tile $0000 excluded), per-level FG1/FG2/FG3/BG1 + SP1/SP2/SP3/SP4 GFX file numbers resolved through OBJECTGFXLIST/SPRITEGFXLIST with the Super GFX Bypass applied (same resolution as the Level GFX Slots browser, so ExGFX files appear under their real indices), and the editor custom-palette flag — plus per-submap overworld music and the installed-ExGFX list. Tools > "Analyze Resources in Levels..." runs it on a worker thread with a progress bar + cancel, over scan-option checkboxes (sprites / Map16 / GFX / music / palettes). Results are shown two ways: by resource (pick a music track, sprite, Map16 tile, or GFX/ExGFX file and see every level using it) and by level (per-level summary table); double-clicking a level jumps it open in the level editor (reusing its unsaved-changes flow). "Save Text Report..." writes the LM-style text report, including installed-but-unused ExGFX files. Verified: unit tests + real-ROM ignored tests (vanilla: 512/512 levels scanned, all 8 music tracks in use, known submap music table, no ExGFX, no custom palettes). See `docs/screenshots/analyze-resources.png` |
| Share data between levels to save space (LM v3.50) | ✅ | `crates/smwe-rom/src/level_sharing.rs` + File > Levels > "Share Data Between Levels to Save Space...": scans all 512 levels' Layer 1 (5-byte primary header + objects), sprite (1-byte header + sprites, bank $07), and Layer 2 object blocks for byte-identical copies; repoints duplicates at the lowest-numbered level's block and erases the freed blocks ($FF fill) as free space. Bank-$FF Layer-2 pointers (vanilla background tilemaps shared by 283 levels) are never touched; a block is erased only when no level references it, it parses cleanly, and its range does not overlap a live block; checksum rewritten. Whole-block byte equality (headers included) makes sharing invisible to the game. Save-path guard (`block_shared_with_other_levels`): saving a level whose block is shared relocates to fresh space instead of erasing/overwriting in place — also fixes the pre-existing hazard for vanilla's already-shared TEST-level blocks. Creates a "Before share data" restore point first. Verified: synthetic unit tests + real-ROM ignored tests (every level loads byte-identical data after sharing; second run is a stable no-op; artificial duplicates merge and their bytes are reclaimed; checksum valid). See `docs/screenshots/share-data.png` |
| Open Level from Address (LM v1.11) | ✅ | File > "Open Level from Address...": prompt for a PC address in hex ("PC address to open level (in hex)", optional `0x` prefix — same label as LM 3.63's help). `src/level_address.rs` decodes the Layer-1 object stream at the address (must end with `$FF`, else an error toast); the stream is spliced into a scratch ROM as the current slot's Layer-1 block (reusing `find_free_space`/`insert_level_layer1` semantics) and decompressed with the real game code, so the canvas renders what LM would. Sprites, entrances and background stay from the current ordinary level (LM loads none of them from the address), and the displayed level number stays the current slot — the left panel shows "Layer 1 ← PC $XXXXX (not in pointer table)". The source bytes are never touched; the next save inserts the imported Layer 1 into the current slot via the normal save path. LM v1.11's own example address 0x30338 (headerless PC of the boss-test-room Layer-1 stream: 33 objects + $FF, verified on the real ROM) is the default in the headless screenshot. Verified: synthetic unit tests + real-ROM ignored tests. See `docs/screenshots/open-level-from-address.png` |

## Overworld Editing

| Feature | Status | Notes |
|---|---|---|
| Submap viewing/navigation | ✅ | `world_editor/mod.rs::load_submap` |
| Layer 1 tile paint/erase | ✅ | `editing.rs` |
| Layer 2 tile paint/erase + repoint on save | ✅ | `write_overworld_l2_stream`, `patch_snes_pointer` |
| Save/repoint to ROM | ✅ | `find_free_space`, `patch_snes_pointer` |
| Path tile drawing (the visual road/dots) | ✅ | Confirmed path tiles are ordinary L1 tiles (SMWDisX has no separate path-data table for the general case) — already fully paintable via the existing L1 draw/erase tools. Only a curated "path piece" picker/auto-tile UX is missing, not the underlying capability |
| Path movement data (LineGuide-style step tables) | N/A | Investigated and ruled out as a separate system for the general case — SMW's ~10 hard-coded special-case connections (`HardCodedOWPaths`/`OWHardCodedTiles`/`OWHardCodedDirs`, `bank_04.asm:~1646`) are the only exception, not worth building UI for |
| Event tile preview toggles (which "destruction" events are shown) | ✅ | `crates/smwe-rom/src/overworld/mod.rs::OverworldEvents` parses the real reveal-tile-swap tables (`$04D85D` tile offsets, `$04DA1D`/`$04DA33` before/after tile IDs, ported from SMWDisX `CODE_04DA49`) + tests; `world_editor` now has a per-event checkbox panel (`events_panel`) replacing the old blanket "activate all events" hack — toggling writes real `OWEventsActivated` WRAM bits so the actual emulated game code applies the swap. Verified against the real ROM (`render_ow_submap --dump-events`: 49 tiles change when all events applied, matching expected castle/fortress/switch-palace reveal graphics) |
| "Edit Reveal Tile List" (LM v2.30) | ✅ | `crates/smwe-rom/src/overworld/reveal_list.rs::RevealTileList`: parses the 22-byte before/after lists at SNES `$04DA1D`/`$04DA33` and writes them back in place (no relocation; untouched ROMs stay byte-identical). `events_using_row` finds every event whose tile at its `$04D85D` offset matches a row's before-ID — the same match `CODE_04DA49` performs. UI: new "Edit Reveal Tile List…" dialog in the world editor with 22 hex `Before`/`After` spinners, per-row usage counts, and a switch-palace note on row 21 (which also writes the tile after the event's offset). Edits are undoable and the map preview re-applies the destruction events with the edited table immediately (`refresh_event_preview`), so new reveals are visible without saving. Verified against the real ROM (ignored round-trip test; vanilla bytes checked). See `docs/screenshots/ow-reveal-tile-list.png` |
| Mario's starting position (LM v1.60) | ✅ | Part of the 22-byte `$009EF0` `InitPlayerOverworldData` table: `crates/smwe-rom/src/overworld/start_positions.rs::OverworldStartPositions` parses both players' submap, animation words (preserved on save), pixel X/Y and tile X/Y (invariant `tile = pixel >> 4` enforced by `set_tile`/`set_pixel`). Saved back in place on Ctrl+S (only when changed; untouched ROMs stay byte-identical). UI: "Starting Positions" section in the world-editor left panel with submap combo, tile X/Y spinners (pixel coords shown read-only), and click-to-place arming on the main-map canvas; M/L markers drawn on the main map only when the player's submap matches. See `docs/screenshots/ow-start-positions.png` |
| Luigi's separate starting position (LM v1.90) | ✅ | Same table and same UI — the game stores a full second copy for Luigi (submap, pixel, tile), not a mirror of Mario's; each player's submap is stored independently |
| Overworld submap music selection (LM v1.30) | ✅ | `crates/smwe-rom/src/overworld/submap_music.rs::SubmapMusic`: parses the 7-byte `OverworldMusic` table at SNES `$048D8A` (read at overworld init, `CODE_048E38`) and its mirror `OverworldMusic2` at `$04DBC8` (read on submap swap, `CODE_04DBCF`); both tables verified byte-for-byte against a real ROM (`[2, 3, 4, 6, 7, 9, 5]`). UI: "Overworld Submap Music…" in the world-editor left panel — one combo box per submap (tracks 2–10, SMWDisX `!BGM_*` names). Both tables are written in place and in sync on Ctrl+S (only when changed; untouched ROMs stay byte-identical); `tables_in_sync` detects mirrors edited by foreign tools. Undoable via the world-editor undo payload. See `docs/screenshots/ow-submap-music.png` |
| Event *ownership* editing (which level/action triggers which event) | ✅ (beyond LM) | `crates/smwe-rom/src/overworld/event_ownership.rs`: parses the 93-byte events-by-translevel table `DATA_05D608` (SNES `$05D608`, PC `0x2D608`) — the game reads it into WRAM `OverworldEvent` on level completion (`bank_05.asm`: `LDY.W TranslevelNo` / `LDA.W DATA_05D608,Y`). Each byte is the triggered event (`0..OW_EVENT_COUNT`), `$FF` = no event. All vanilla values validated (none out of range). UI: new "Event ownership (by level)" panel in the world editor with one combo per translevel — `None (no event)` or `Event N` annotated with the tile it reveals (from the real reveal-tile table) — plus level names from the name-table work. Out-of-range events are refused, not clamped. Save writes the table back in place (no relocation patch needed); untouched ROMs stay byte-identical. Verified against the real ROM incl. an ignored round-trip test (`real_rom_event_ownership_table`). See `docs/screenshots/event-ownership.png` |
| Layer 2 event tiles (separate from Layer 1 reveal-tile swaps) | ✅ | `crates/smwe-rom/src/overworld/mod.rs::OverworldL2Events` parses the real tables (SMWDisX `bank_04.asm`): the 371-entry 4-byte table at `$04DD8D` (`data_word`/`dest_word`), the 121-word cumulative per-event boundary table at `$04E359` (event `e` owns entries `boundaries[e]..boundaries[e+1]`; driven by `CODE_04E453`/`CODE_04E6D3`/`CODE_04E6F9`), and the 44-row "silent event" tables (`$04E8E4` event numbers, `$04E910` flags bit 0 = L2 vs Map16 edit, `$04E93C`/`$04E994` dest/data words, `CODE_04E9EC`). Entry kind decoded from `data_word` (`< $0900` = VRAM tile stream via dynamic stripe, `>= $0900` = tilemap copy from WRAM `$7F8000+data_word`); target tile decoded exactly like the game (`x_px = ((dest & $3E) << 2)`, `y_px = (((dest >> 3) as u8) & $F8)`). UI: new "Layer 2 events" section in `world_editor`'s events panel lists per-event entry ranges with kind + target tile (and silent rows), plus on-map target markers (cyan = tile stream, orange = tilemap copy) that follow the event checkboxes. Verified: real-ROM ignored test matches the disassembly byte-for-byte (first entries, boundaries, silent rows); all 13 of event 1's entries hand-checked against the ASM bytes. See `docs/l2_event_tiles.md` and `docs/screenshots/l2-events-map.png`, `docs/screenshots/l2-events-panel.png` |
| Level-number display per tile (read-only, vanilla-accurate) | ✅ | `crates/smwe-rom/src/overworld/mod.rs::level_number_at`/`translevel_at` (+ tests), surfaced in `world_editor/mod.rs` tile-inspect panel. Confirmed against real ROM (`render_ow_submap --dump-levels`): matches vanilla's translevel scan-order numbering including the documented 0x25→0x01 wraparound |
| Level-number free reassignment (LM-style, arbitrary) | ✅ | Turned out not to need code injection: a single existing instruction (`LDA.L $7ED000,X` at SNES `$05D89B`, confirmed byte-for-byte against a real ROM: `BF 00 D0 7E`) is repointed to a custom ROM table instead of the vanilla WRAM-computed one, using the same `layer1_tiles` index space. `encode_custom_level_number` inverts the vanilla remap so the existing (unmodified) remap code still produces the right final number; verified with an exhaustive round-trip test over all 220 representable values (0x00-0xDB). UI in `world_editor/mod.rs` tile-inspect panel; only touches the ROM if the user actually overrides a level number, so untouched hacks stay byte-identical to vanilla in this area. Known limitation: repeated saves with active overrides allocate a fresh table each time rather than reusing one in place (documented in code, harmless but wasteful) |
| Custom level names (overworld name table) | ✅ | `crates/smwe-rom/src/overworld/level_names.rs`: decodes the 93-entry `LevelNames` table (SNES `$04A0FC`) via the three fragment tables (`$049C91`/`$049CCF`/`$049CED`, `CODE_049D07` in `bank_04.asm`). The vanilla 460-byte string pool is 100% full, so customized names trigger a relocation patch: tables move to `$FF`-filled space at `$04A1B6` (93/16/16 slots; T2/T3 capped at 16 by the 4-bit packed entry format), pool expands in place to 578 bytes, and three `LDA.W` address constants are rewritten. Encoder splits names into shared (prefix, middle, suffix) fragments, merging rare fragments into T1 to fit the slot caps. UI: text field in `world_editor` tile-inspect panel per translevel, vanilla name as hint, and byte-budget enforcement (`check_name`: at most 19 tiles — the game's `$26`-byte stripe buffer — `A–Z 0–9 space # '` only; over-long/invalid input refused with a red error, mirroring the message-box editor). Verified: all 93 vanilla names decode correctly; encode→patch→decode round-trip passes on the real ROM. See `docs/custom_level_names.md` and `docs/screenshots/custom-level-names.png` |
| Layer 2 scroll properties (not raw tiles) | ✅ | `crates/smwe-rom/src/level/scroll.rs`: named paired presets 0-15 (vanilla 0-7 verified against SMWDisX `DATA_05D710`/`DATA_05D720`; LM 3.00 additions 8-11 = H:None/V:Medium 2/3/4/Slow 2; 12-15 blank as in LM) replacing the old 0-15 numeric slider, plus LM 3.40 separate horizontal/vertical mode via the `$06FA00` extension byte (`SHCvvvvv`: S=separate, H=horizontal auto-scroll flag, C=auto-set screens, vvvvv=vertical 0-31). H dropdown: 9 speeds + Fast + 12 Left/Right auto-scrolls; V dropdown: 9 speeds + Fast + 12 Up/Down auto-scrolls. `SecondaryHeader.scroll_ext` round-trips the byte; vanilla ROMs keep `$FF` (uninstalled) until separate mode is enabled. MWL level-info byte 17 carries `$06FA00` (per the v3.63 MWL spec). UI in `left_panel.rs` (paired combo + Separate H/V checkbox + H/V combos + Auto-Set Screens). LM 3.40 Auto-Set Number of Screens on-save behavior: when the per-level C bit is set and the table is installed, `save_to_rom` rewrites header byte 0 to the screens actually occupied by objects/exits/sprites (`src/ui/editor_prototypes/level_editor/auto_screens.rs`; anchor-tile scan along the long axis, min 1 / max 32, exits by screen number; documented approximation for wide objects). Touching any scroll-extension control installs the table on vanilla ROMs (tracked by `scroll_ext_dirty`); untouched `$FF` ROMs are never resized. In-memory header state is re-synced in `on_save_succeeded`. See `docs/screenshots/layer2-scroll-modes.png`, `docs/screenshots/auto-set-screens.png` |
| Animated overworld tiles (preview) | 🟡 | `smwe-emu::emu::{init_ow_anim_water, advance_ow_anim_frame}`: Rust transcription of `CODE_048086`/`OW_Tile_Animation` (`bank_04.asm`). Initializes the 96-byte water/waterfall buffer at `$7E0AF6` from decompressed GFX14 (`GFX14_OWAnimation`) via `DATA_048000` pointers (`$7EB480/$7EB498/$7EB4B0`), then rotates bits per visible frame (8 game-frames = 133ms). VBlank DMA replica uploads 352 bytes to VRAM word `$0750` (4bpp tiles 117–127). World editor ticks every 133ms and re-uploads. Editing the animation data (not just previewing) is not implemented — no LM-parity editable table was found. Indicator sprites were investigated: `OWScrollArrowStripe` is a fixed border graphic, and Mario/OW sprites are gameplay rendering, not an LM-style editable indicator list. See `docs/screenshots/ow-animated-tiles.gif` |
| Overworld undo/redo | ✅ | Added 2026 (commit `77dfc73`) |
| Editable overworld ExAnimation | 🟡 | `crates/smwe-rom/src/exanimation.rs` (new `overworld` list on `ExAnimationData`; RATS payload format v2, v1 blocks still decode) + `src/ui/exanimation_dialog.rs` shared "ExAnimated Frames (Overworld)" window in the world-map editor (same frame types/editing as the level dialog: line 8x8/16x16, palette, palette-rotate, triggers, VRAM tile browser, disable-original flag). Live preview: the world view ticks every ~133ms, applies VRAM/CGRAM updates through the same `apply_tick`, and can suppress the vanilla OW water animation. Saved back to the ROM's `SMWEXAN1` block on world-editor save. Honest limits: authors + previews only — in-game playback needs LM's overworld ExAnimation ASM (v2.40 hack, not installed by this editor). Verified by unit + real-ROM ignored tests (incl. v1→v2 compat). See `docs/screenshots/ow-exanimation-preview.png` and the animated `docs/screenshots/ow-exanimation-preview.gif` |
| Boss sequence text editor | ✅ | `crates/smwe-rom/src/boss_text.rs` + `level_editor/boss_text_editor.rs` — all 53 boss-defeat cutscene messages (7 bosses × 7–8), verified against the real ROM (stripe blobs at `C1Message1Stripe`…`C7Message8Stripe` in bank $0C, SNES $0CBE85–$0CC92D; big-endian VRAM dest + flags/len, LE tile words, $FF terminator). Messages decode to real text ("Mario has defeated the demented Iggy Koopa in castle #1…", "Ludwig von Koopa's days of composing Koopa symphonies in castle #4…", Roy's "the dangerous "but tasty" Chocolate Island!" with real open/close quotes). Single-line text field encodes live to stripe tiles (`$39TT`); fixed-size blobs so shorter text space-pads and over-long input is refused. Boss font = `FontMap::real()` + `#` ($5A), `0-9` ($63–$6C), `"` ($5B/$5B) — all verified against real GFX2A tiles. Clear Text / Clear All Text buttons (LM v3.20). True-font raster preview per message. See `docs/screenshots/boss-text.png` |
| Overworld sprite editing mode | ✅ | `crates/smwe-rom/src/overworld/sprites.rs`: parses/writes the 13 fixed vanilla sprite slots ($04F625: number + LE x/y) plus the per-sprite-*number* map visibility bytes ($04F828, indexed by sprite dispatch number per `CODE_04F87C` — a set bit means *hidden* on that submap; verified against SMWDisX and the real ROM: e.g. the Valley of Bowser sign (sprite 08) is VoB-only, Yoshi's House smoke (07) is main-map-only). Custom sprite insert/delete/storage keyed off the LM v2.50 `$0EF55D` pointer and its entry layout (7 submap offset lists, ≤24 sprites each; 7-bit number, 6-bit 8px X/Y, 5-bit height, variable extra bytes; extra-byte counts from the `$0DE18C` marker table) — but the container is a smw-editor-owned `OWSPRITE` RATS block, not a byte-for-byte replica of LM's native container (its exact internal layout isn't publicly documented), so tables authored by other tools are detected and never overwritten. World-editor "Sprites [4]" tool (`src/ui/world_editor/sprite_tool.rs`): map markers (yellow vanilla / cyan custom, white = selected, same canvas basis as the GL render), click-select, drag-move (vanilla pixel-exact, custom snaps to 8×8 units, one undo step per drag), per-number submap visibility checkboxes, custom number/X/Y/height/hex extra-bytes editor (byte count fixed by the ROM's extra-byte table). Honest limit: custom sprites need a third-party runtime patch (e.g. PIXI) to act in-game — smw-editor stores the table, it doesn't run the sprites. Verified by unit + real-ROM ignored tests. See `docs/screenshots/ow-sprites.png` |
| Custom overworld sprite record sizes (LM v3.51) | ✅ | `SpriteSizeTable` (`crates/smwe-rom/src/overworld/sprites.rs`): one total record size per sprite number `01..=7F` (127 entries), each 3–15 total bytes (3 = fixed bytes only, no extra bytes; 15 = max; default 4) — Lunar Magic v3.51 (2024-12-25) "added support for a sprite size table for custom overworld sprites" (verified against the official LM 3.63 help file 2026-09-18; LM 3.60 changed the table from 0x80 entries starting at sprite 0 to 0x7F starting at sprite 1). Table lives at the 3-byte pointer `$0DE18C` with marker byte `$42` at `$0DE18F`. Per-sprite extra-byte counts derive as `size - 3` (sprite 0 has no entry, keeps the default). New "Sprite Sizes…" dialog in the world-editor sprite panel (`src/ui/world_editor/sprite_tool.rs`): per-sprite total-size drag fields (3–15) with derived extra-byte readout, single-undo-step Apply that resizes every placed sprite's extra bytes to match. On a ROM without a table, Apply creates one (RATS-tagged free-space block, `$0DE18C` pointed at the data, `$42` marker); existing tables (LM/3rd-party-authored or ours) are edited in place. Tracked independently of the `$0EF55D` custom-sprite container in edit state and undo. Honest caveat, straight from LM's help: Lunar Magic only *reads* the table — sizes do nothing in-game without a third-party runtime patch, so the editor stores the table as data, not behavior. Verified by unit tests (defaults/validation, sprite-1 indexing, total→extra conversion, create/parse/write round trip, raw LM-style table in-place edit, clamping, custom round trip with variable sizes) + real-ROM ignored tests. See `docs/screenshots/ow-sprite-size-table.png` |

| Secret Exits 2/3 direction-to-enable (LM v3.00) | 🟡 | `crates/smwe-rom/src/overworld/secret_exits.rs`: per-level direction-to-enable masks for Secret Exit 2 and Secret Exit 3, using the vanilla `OWLevelTileSettings` direction-bit convention verified against SMWDisX `bank_04.asm` (`$01` right, `$02` left, `$04` down, `$08` up). The vanilla ROM has no per-exit direction table (directions come from event activation + runtime state: `InitLevelTileMovementData`, `DATA_04941E`), and LM's exact on-disk format isn't public, so the settings live in an editor-owned `SMWSEXIT` RATS block (versioned payload; block found by tag scan, all matching blocks erased on save so repeated saves leave no orphans; untouched ROMs keep no block). World-editor "Secret Exits 2/3…" window (`src/ui/world_editor/secret_exits.rs`): level picker (0x000–0x1FF), Up/Down/Left/Right checkboxes per exit, per-level list, undoable, saved with the world map. Matching goal tapes: the level editor's sprite picker now names sprite `$7B` with Extra bits 2/3 as "Goal Point (Secret Exit 2/3)" (`sprite_catalog.rs`: `sprite_variant_name`), matching LM v3.00 adding those sprites to its sprite list — the exit mode really is `extra_bits + 1` (SMWDisX `bank_00.asm`: `SecretGoalTape = extra_bits`, `OWLevelExitMode = SecretGoalTape + 1`). Honest limits: real Secret Exit 2/3 in-game behavior needs LM v3.00's ASM (not installed); on a stock ROM extra bits 2/3 hit vanilla's quirky exit-mode paths. Verified: `cargo test -p smwe-rom` (codec round-trips, orphan-free rewrites, corrupt-version rejection) + real-ROM ignored test (vanilla ROM parses to empty settings, no false-positive tag hits). See `docs/screenshots/ow-secret-exits.png` |

## Graphics / Map16 / Palette Tools

| Feature | Status | Notes |
|---|---|---|
| VRAM/GFX viewer widget | ✅ | `crates/smwe-widgets/src/vram_view.rs` |
| Palette viewer widget | ✅ | `crates/smwe-widgets/palette_view.rs` |
| Map16 editor | ✅ | `map16_editor.rs` |
| Map16 page import/export | ✅ | `crates/smwe-rom/src/map16_file.rs` + `level_editor/map16_file.rs` + `src/bin/render_map16.rs` — export/import Map16 pages (FG pages 0-1 per tileset 0-4, BG pages 0-1) as raw 0x800-byte files that are Lunar Magic `Map16Page.bin` compatible (verified interchangeable shape: no header, same 8-bytes-per-tile layout). Import writes the pages' fixed vanilla ROM addresses in place (no repointing). Round-trip verified against the real ROM: export → import → re-export is byte-identical for FG pages (tilesets 0 and 2) and BG pages. UI lives in the Map16 Block Editor window (page/tileset selectors, Export page / Import, status line) |
| Expanded Map16 (0x80 FG + 0x80 BG pages) | ✅ | `crates/smwe-rom/src/map16_expanded.rs` — pages 0x02-0x7F (FG) / 0x00-0x7F (BG) per Lunar Magic 1.70/2.50. Expanded FG pages resolve through LM's own pointer table (`$06F553`+ ranges) when present; editor-created pages live in one RATS block per page (`SMW16FG<p>` / `SMW16BG<p>` tags, 12-byte metadata + 0x800 page) so all 0x7E pages fit without crossing LoROM bank boundaries. UI: FG/BG radio + 0x00-0x7F page slider in the Map16 Block Editor (block slider widened to 0x7FFF); the vanilla `map16_block_ptrs` cache was resized to 0x8000 so expanded blocks render. See `docs/screenshots/map16-expanded.png` |
| Block editor: "acts like" (LM 1.91) | ✅ | `crates/smwe-rom/src/map16_expanded.rs` — 16-bit Act As entry per FG tile 0x0000-0x7FFF (BG has none, as in LM); blank = acts as itself. Stored in the editor-owned sparse RATS table `SMW16ACT` (custom tag: LM's exact ROM representation is relocatable low/high-byte planes + runtime ASM — see caveat below). UI: per-selected-block "Acts like" slider in the Map16 Block Editor with Reset, plus a Remap dialog with LM's exact syntax forms (`G100-101,+25` shift, `G100-101,M125`/`125` map-onto, `R200-211,S25` assign-range-from-base) and a real preview of affected tiles; user-entered gameplay values are clamped to < 0x200 like LM. Written on save; source values always refer to pre-remap values, as in LM (LM never auto-rewrites references when blocks move). **Caveat:** non-identity act-as values need compatible runtime support to affect gameplay (LM gets this from its expanded-Map16 ASM) — this PR preserves and remaps the data faithfully |
| Modern `.map16` format (LM 1.90+) + export/import ALL | ✅ | `crates/smwe-rom/src/map16_file.rs` — exact 0x40-byte `LM16` container (magic, version 0x0100, game 1 = SMW, flags at 0x28, 8-entry offset/size table): partial page/rectangle export (tile data 8 bytes/tile in UL/LL/UR/LR order + FG act-as 2 bytes/tile; FG-relative/BG-relative or legacy absolute coordinates; variable rectangle metadata), and full-game export/import-all (0x80000 tile section for FG 0x00-0x7F + BG 0x80-0xFF, 0x10000 act-as section, 0xF000 tileset-group section). FG pages 0x00/0x01 come from the tileset-group section like LM's own exports. Raw 0x800-byte files still import. UI: Export page… (modern format), Export ALL…, multi-file Import… (partial pages land on the file's location or the selected page; rectangles; full files replace/clear all data). See `docs/screenshots/map16-expanded.png` |
| Vanilla GFX file reading (0x00-0x33) | ✅ | `crates/smwe-rom/src/graphics/gfx_file/` — decompresses into `rom.gfx.files` for VRAM composition |
| GFX write plumbing (compress + tile encode + repoint) | ✅ | `compression::lc_lz2::compress` (direct-copy + byte-fill, verified round-trip against real ROM GFX data) + `GfxFile::to_raw_bytes`/`decode_tiles` (tile encoders, exact inverse of the existing decoders, verified round-trip against real ROM data) + pointer-table read/repoint logic in `level_editor/mod.rs::save_to_rom` |
| ExGFX import/export UI | ✅ | `level_editor/gfx_editor.rs` — export any GFX file slot (0x00-0x33) as a lossless grayscale PNG (pixel intensity = color index, not a colored preview) via `rfd` file dialogs; import a PNG back, staged in `gfx_edits` and written on save (LC_LZ2-compressed, repointed via `find_free_space` if the new data doesn't fit in place). Verified end-to-end against real ROM GFX file 0 (export→import→re-encode reproduces the original bytes exactly, and compresses/decompresses correctly) |
| Colored (palette-aware) GFX preview/import, per-level GFX slot *browser* tied to the editor | ✅ | Per-file import is still grayscale-index-only (deliberate, for exact round-tripping), but the new 8x8 tile editor (`level_editor/tile_editor.rs`) adds the palette-colored WYSIWYG view: every tile of a GFX file rendered through a selectable 16-color CGRAM row. The new "Level GFX Slots" window (`level_editor/gfx_slot_browser.rs`) is the per-level slot browser: it resolves the level's FG/BG GFX nibble through OBJECTGFXLIST ($00A92B, 26 rows — the `CODE_00AA35` upload loop) into FG1/FG2/FG3/BG1 with GFX file numbers + VRAM tile ranges, and the Sprite GFX nibble through the new `smwe_rom::objects::sprite_gfx_list::SpriteGfxList` ($00A8C3, 26 rows — `UploadSpriteGFX` in `bank_00.asm`, verified byte-for-byte against the disassembly and the real ROM) into SP1/SP2/SP3/SP4 with file numbers + VRAM ranges. Every slot row has an Edit button that opens the 8x8 tile editor on that GFX file with the level's FG (FG/BG slots) or sprite (SP slots) CGRAM palette row preselected, so tiles are colored exactly as the level shows them; "Slots…" buttons next to the FG/BG GFX and Sprite GFX sliders in the Level Header window open the browser. See `docs/screenshots/gfx-slot-browser.png` |
| 8x8 tile bitmap import/export | ✅ | Replaced by a real pixel editor, not a bitmap pipe: `level_editor/tile_editor.rs` shows a 16-column palette-colored grid of every tile in any GFX file (2/3/4/8bpp-aware), a selectable CGRAM palette row, a zoomed 8x8 pixel canvas with left-drag painting + right-click eyedropper, and Apply stages the edited tile back through `GfxFile::to_raw_bytes` into the existing `gfx_edits` save path (LC_LZ2 + repoint on save). Double-clicking a Map16 sub-tile preview opens the editor on that tile's source GFX file and selects the tile. Round-trip verified: painted pixels re-encode with byte diffs confined to the edited tile; real-ROM ignored test confirms tileset 0 maps to OBJECTGFXLIST row `Normal 1 = [0x14, 0x17, 0x19, 0x15]` |
| True ExGFX support: extra graphics files + Super GFX Bypass + per-level assignment | ✅ | `crates/smwe-rom/src/exgfx.rs` — LM v1.10-style ExGFX import (exact-32 KiB 4bpp files, indices 0x80-0xFFF, stored in documented `SMWEXGFX` RATS blocks with per-level file references in MWL section 7) plus the Super GFX Bypass window (`level_editor/gfx_bypass.rs`): FG1/FG2/FG3/BG1/SP1/SP2/SP3/SP4 slots independently assignable per level to any vanilla file or ExGFX file, honoring LM's per-slot format rules. Vanilla slots upload through the real `UploadGFXFile` game routine (3bpp-to-VRAM conversion via emulator), ExGFX slots copy directly. Storage uses a `SMWGFXBP` RATS block rather than LM's hidden level-header bits, so it survives ROM round-trips. See `docs/screenshots/exgfx-manager.png` + `docs/screenshots/gfx-bypass.png` |

## Sprites

| Feature | Status | Notes |
|---|---|---|
| Sprite placement in levels | ✅ | See above |
| Sprite extra bits (2-bit position field per sprite) | ✅ | `sprite_layer.rs`, `left_panel.rs:103` — confirmed editable, corrected from an earlier pass that missed it. **Extension-byte variant tooltips (LM v3.30)**: hovering a sprite picker entry shows its ID, name, and one line per configured extra-bit variant (`sprite_catalog.rs::sprite_tooltip`, e.g. Goal Point lists "Extra bits 2: Goal Point (Secret Exit 2)"), matching LM v3.30's separate appearances/tooltips keyed on the lower bits of a single extension byte. The in-game variant model still reads only vanilla extra bits, so unconfigured variants remain appearance-identical — named but not yet visually distinct |
| Sprite Map / OAM tile editor | ✅ | `src/ui/editor_prototypes/sprite_map_editor/` |
| Sprite tweaker/behavior byte editing (6 global tables, "Sprite Header Editor") | ✅ | `crates/smwe-rom/src/sprite_tweakers.rs` parses the 6 ROM tables ($07F26C/$07F335/$07F3FE/$07F4C7/$07F590/$07F659, 0xC9 entries each) with named bit accessors (+ tests); `sprite_tweaker_editor.rs` in the level editor exposes all of them with save-to-ROM support. Verified against real ROM: Goomba (0x0F) shows `can_be_jumped_on=true`, `dies_when_jumped_on=false`, matching known vanilla behavior. Edits are global (affect every placement of that sprite ID), matching how LM's own editor works |
| Sprite category distinction (cluster/extended/generator vs. normal) | 🟡 | `SpriteTweakers::has_tweakers()` now encodes the boundary (IDs `>= 0xC9` don't have tweaker bytes) and the tweaker editor warns when selecting one; no dedicated `SpriteCategory` type or separate editing UI for those categories yet |
| Per-level sprite header — "Change Properties in Sprite Header" (vanilla fields + LM 3.00 options) | 🟡 | Toolbar ghost button → dedicated window: sprite memory setting 0x00–0x12 (LM's dropdown stops at 0x09, but vanilla levels use up to 0x12 — 19-entry `SpriteSlotMax/Start` tables, `bank_02.asm`; bit layout verified in `bank_05.asm`: `AND #$3F` → `$1692`, `AND #$C0` → `$190E`, bit 5 never set by any vanilla level per real-ROM scan), buoyancy (bit 7), disable Layer 2 interaction (bit 6); the dialog writes the edited byte on save (previously the original byte was always preserved) with session-authoritative per-level state. LM 3.00 options — sprite vertical spawning range (Normal/Wide/Wider/Widest) and smart spawning — plus a sprite-count readout against LM 3.00's 128-sprite cap (non-SA1; warn when exceeded). Honest limits: the two LM 3.00 options are stored in an editor-native RATS block (`SMWSPRH1`, `crates/smwe-rom/src/level/sprite_header_ext.rs`, same pattern as the LM 3.00 level-height slice) — they need LM 3.00+'s sprite engine to take effect in-game and are inert on a stock ROM; the header-byte fields work on any ROM. See `docs/screenshots/sprite-header-editor.png` |
| Custom sprite insertion (SA-1/UberASM-style dropins) | ⛔ | Not found |

## Music / Sound

| Feature | Status | Notes |
|---|---|---|
| Music track selection (header nibble) | ✅ | Named picker with all 8 vanilla tracks (SMWDisX `LevelMusicTable`); raw byte shown alongside for custom/remapped ROMs |
| Music/SPC data import or editing | ⛔ | Not found |

## Data / ASM / Patches

| Feature | Status | Notes |
|---|---|---|
| Restore points + create/apply IPS (Restore menu) | ✅ | LM v1.80 parity: new **Restore** menu in `src/ui/mod.rs` with **Create Restore Point...** (named dialog), **Revert to Restore Point** submenu, **Create IPS Patch...** / **Apply IPS Patch...**, and a "Track changes (restore point before each save)" toggle. `src/ui/restore.rs` `RestoreManager` keeps a reference copy of the ROM as opened plus named full-image snapshots (512 KiB each); manual points are never pruned, automatic pre-save points are capped at 20 FIFO. Reverting/applying writes the image atomically with a `.bak` backup, reloads the project, closes stale editor tabs (they hold `Arc<SmwRom>`s parsed from the old image), and reopens the level editor. `smwe-ips` gained `apply_patch` (raw + RLE records, EOF, trailing truncation size, 24-bit address space, zero-RLE-count = 65536) with create→apply round-trip tests. IPS create now warns when the patch lands in the same folder as the ROM (LM v1.80). Honest limits: revert/apply close open editors rather than hot-swapping their parsed state; LM's "Check Object Placement on Save" and sprite-count save warnings are not part of this slice. Verified: `cargo test -p smwe-ips` (15 tests incl. foreign-tool patch layout) + new `ui::restore` unit tests + real-ROM ignored-suite shape. See `docs/screenshots/restore-menu.png` and `docs/screenshots/apply-ips.png` |
| ASM insertion tool / hijack manager | ⛔ | No user-facing ASM editor |
| 65816 disassembler | ✅ (library only) | `crates/smwe-rom/src/disassembler`, `crates/wdc65816` — not exposed as a user-facing ASM editor |
| ROM expansion (expand to 1/2/4MB, freespace tracking) | ✅ | `crates/smwe-rom/src/rom_expansion.rs`: `expand_rom` grows a LoROM image (512KB/1MB/2MB) to 1/2/4MB by appending `$FF`-filled banks, then fixes the internal header (ROM size byte + recomputed checksum/complement pair, map mode untouched); SMC headers preserved on write. UI: File > Expand ROM... dialog with target picker, `.bak` backup, unsaved-edits-saved-first. The appended `$FF` space is picked up automatically by the unified `find_free_space`/`find_free_space_in` scanner used by level layer1/layer2/sprite data, GFX files, message boxes, and overworld L2 — verified with a unit test. See `docs/rom_expansion.md` and `docs/screenshots/rom-expand.png` |
| Title screen editor | ✅ | `Title/Credits…` window exposes fixed-slot title data plus a full-tilemap WYSIWYG editor (LM v1.63/v3.40 parity): the 905-byte `TitleScreenStripe` (logo, rows 0–13) composited with the 85-byte `PlayerSelectStripe` (menu, rows 7–10; RLE clear commands preserved verbatim). Parses both stripes with exact `LoadStripeImage` DMA semantics including RLE (3-byte headers, 32-word vertical stride, odd RLE byte counts), previews in 2bpp (Mode 1 BG3) with real title GFX/palette from the emulator (level 0xEB + `CODE_00ADA6`/`CODE_00922F`), click/drag painting routed by row to the owning stripe, right-click tile picking, tile-word editor, and independent byte-budget meters (logo `used / 1108`, menu `used / 85`; over-budget refused). Japanese ROMs are supported (LM v1.30 parity): the layout is selected from the internal header's region code — J title stripe at `$05AF2C` (893-byte slot), J player-select stripe at `$05B358` (91-byte slot), J input sequence at `$009BB4` (`$8E`-byte slot), J title-submap operand at `$009663`; window title and budget meters follow the active layout. Screenshot: `docs/screenshots/title-full-tilemap.png`. Title-move playback data export/import (LM v1.91 parity): the Demo-input row has "Export title moves…" / "Import title moves…" buttons saving/loading a documented `.smwtm` file (magic `SMWTMV1`, region code, length-prefixed raw `$FF`-terminated input-sequence payload, byte-identical to the ROM slot bytes); import validates magic/version/structure, refuses over-budget payloads, and warns on region mismatch. Screenshot: `docs/screenshots/title-moves-export.png` (real demo steps + real file header). Honest limit: LM v2.30's Snes9x-savestate *recording* of title moves is not implemented — that requires an external emulator integration; the playback-data format it exports is what this covers. |
| Credits editor | ✅ | `Title/Credits…` window has a full-area WYSIWYG editor for all 13 ending enemy-name stripes (`EnemyNameStripe00..0C`, LM v3.40 parity): parses each stripe with exact `LoadStripeImage` semantics (including RLE), splits Layer-3 text commands (VRAM $5000+, rows 0–13, 64×14 editable canvas covering the full viewable area) from preserved non-L3 background commands, previews with the real GFX2F 2bpp credits font, click/drag painting, right-click tile picking, tile-word editor, and per-scene byte-budget enforcement (over-budget refused). Japanese ROMs are supported (LM v1.30 parity): the 13 J enemy-name scenes live at `$0DF300..$0DFCEB` with the per-scene J slot sizes from the `ver_is_japanese` disassembly branches; J scenes are listed by number (the J credits text is katakana — no Latin decode is implemented). Non-L3 commands are preserved verbatim on save. Honest limit: the J Special-World name-update stripes (U-only feature) and boss-sequence text (different layout on J ROMs) are not supported. Screenshot: `docs/screenshots/credits-full-area.png`. Staff roll text/scripts, credits scene scripts, HDMA, sprite choreography, and ending special enemy-name overlays are not modeled yet |

## Save / Export

| Feature | Status | Notes |
|---|---|---|
| Save to ROM | ✅ | `src/ui/mod.rs::save_rom`/`save_rom_as`/`write_rom_to_path` |
| SMC header detection on save | ✅ | `write_rom_to_path` |
| Check Object Placement on Save (LM v1.91) | ✅ | **Options menu** toggle, persisted per-user in `$HOME/.smw-editor-options.json` (never the ROM). `src/placement_check.rs`: level bounds (horizontal 16×screens × 27, vertical 32 × 16×screens), vanilla objects by anchor tile (sprites in vertical levels by screen number, since the game ignores y_tile for Y), Direct Map16 objects by real w×h footprint; screen exits and screen jumps are control records and are not checked. The live check runs against each level-editor tab's current (unsaved) edit state via `DockableEditorTool::placement_issues`, using the same effective screen count the save path writes (so the LM 3.40 Auto-Set Number of Screens recompute never warns about what the save itself fixes). Saving with the toggle on shows an "Object Placement Warning" dialog listing each issue with its level; like LM it warns, it does not block ("Save anyway" / "Cancel"). Unit-tested bounds incl. real-ROM ignored test asserting vanilla levels are clean except for the documented vanilla $108 OBJLedge quirk. See `docs/screenshots/check-object-placement.png` |
| Repointing / freespace allocation | 🟡 | Free-space *scanning* is now unified in `src/rom_freespace.rs` (with tests), used by level L1/L2/sprite data, GFX files, message boxes, and overworld L2 — previously duplicated verbatim in two places. Writing the new pointer bytes themselves is still feature-specific (different pointer table layouts per feature: 3-byte SNES pointers, GFX's 3-lookup-table split, message boxes' 25-entry u16 table, etc.), which is inherent to the ROM format rather than something to further unify |

## Misc Tools

| Feature | Status | Notes |
|---|---|---|
| Lunar Magic-style level-editor toolbar | ✅ | `src/ui/editor_prototypes/level_editor/toolbar.rs`
| Custom user toolbar (LM v2.31+) | ✅ | `crates/smwe-usertoolbar` (pure parser + model, unit-tested against LM 3.63's own example files) + `src/ui/user_toolbar.rs` runtime: LM-format `usertoolbar.txt` loaded at startup from beside the executable or the per-user config dir (`%APPDATA%\smwe`, `~/Library/Application Support/smwe`, `$XDG_CONFIG_HOME/smwe`); renders a second toolbar strip below the menu bar with external "scripting buttons" (launch with `{rom}` argument substitution + `SMW_ROM` env, one-process-per-button default, `LM_ALLOW_MULT_INSTANCES(_FORCE_ALL)` opt-out, `LM_OPEN_OTHER` via OS file association, executable-relative working dirs), internal `LM_…` command buttons (`LM_VIEW_OVERWORLD` → World Map Editor, `LM_FILE_DELETE_LEVEL` → Delete Levels dialog, `LM_FILE_EXPORT_DIRECTORY_BITMAP` → batch PNG export; unmapped names render disabled), spacers, tooltips (`\n` → newlines), and shortcuts (Windows VK names / quoted chars / hex + Ctrl/Alt/Shift; active even with `LM_NO_TOOLBAR`, skipped while typing). Parse/launch errors surface in a window capped at `LM_DISPLAY_ERRORS`. Honest adaptations: text labels instead of exe-icon extraction; no `$BECA` Windows-message protocol. See `docs/USER_TOOLBAR.md` and `docs/screenshots/user-toolbar.png` |
| Custom tooltips for objects (LM v3.60) | ✅ | `src/custom_tooltips.rs`: per-user JSON store (`$HOME/.smw-editor-custom-tooltips.json`, same convention as recent-files) of user-settable tooltip text for standard and extended object IDs (0x00–0xFF each, trimmed, capped at 256 chars, malformed files load empty). Level editor: toolbar 💬 button opens the "Custom Object Tooltips" manager window (Standard/Extended tabs, hex-or-text search, per-ID edit with Save/Clear, count readout); hovering an object on the canvas shows its tooltip via `on_hover_text_at_pointer` (topmost object wins; only when a tooltip is set); the Edit Manual dialog (LM v1.91) shows the selected object's tooltip under the intro. Tooltips are editor metadata — never written to the ROM. See `docs/screenshots/custom-tooltips.png` |
| Cut/copy/paste clipboard across editors | ✅ | `src/ui/clipboard.rs`: versioned `smwclip:1:` text payloads on the system clipboard (level objects+sprites incl. footprint blocks, Map16 block IDs / tile words, 8×8 tile pixels, overworld layer-1 regions). Level editor: toolbar Cut/Copy/Paste icon buttons + Ctrl+X/C/V (paste anchors at cursor, cascades otherwise; footprint tiles stamp with the pasted objects; object/sprite lists each paste in one undo step). Map16 Block Editor: Copy/Paste block buttons + Ctrl+C/V (tile-word paste; a one-block payload jumps to that block). 8×8 Tile Editor: Copy/Paste tile buttons + Ctrl+C/V. Overworld: Shift+drag region select + Ctrl+C/V (single undoable write). Incompatible cross-editor pastes are refused with a status message. Map16 block patterns paste into the level as Direct Map16 access objects (see below). See `docs/screenshots/clipboard.png` and the animated `docs/screenshots/clipboard.gif` | — horizontal icon toolbar (phosphor icons) mirroring LM's main window: Save-to-ROM / Reload / level-number box / zoom (row 1); Layer 1 / Layer 2 / sprite edit-target toggles, Select/Insert/Erase/Probe tools, grid+overlay+label view toggles, and sub-editor launchers Header/Map16/GFX/Palette/Sprite-behavior/Messages/Secondary-entrances/Title-credits (row 2); plus a bottom status bar (mode · edit target · level · size · selection). Controls were lifted out of `left_panel.rs`, which now holds only the tile/sprite palette + selection inspector. Icon buttons drive existing `UiLevelEditor` state, so all wiring was already functional. Keyboard shortcuts match Lunar Magic (source: SMW Central "Lunar Magic Shortcuts", Thomas, v3.60): PageUp/PageDown step level number, backtick toggles Layer 1/2 editing, F8 toggles grid, Ctrl -/= zoom. **Known divergences from real LM:** (1) the Select/Insert/Erase/Probe tool row is this editor's own interaction model — LM has no tool palette (it uses left-click=select/move, right-click=paste-place, Delete=erase gestures instead); (2) the grid/overlay/label view toggles don't map to LM's per-layer 1/2/3/4/6 view keys; (3) exact button order and pixel-art icons aren't matched (LM skins its toolbar via a `Lunar Magic.ff4` 16×16-button bitmap). **Follow-up:** Undo/Redo buttons not added — `UndoableData` supports undo but object edits paint straight into the WRAM block map with no "re-rasterize object list → WRAM" path, so undo would revert the model while leaving the canvas stale; needs that raster path first |
| Address converter (PC/SNES) | ✅ | `src/ui/dev_utils/address_converter.rs` |
| Welcome screen / ROM open flow | ✅ | `src/ui/welcome.rs` and the File menu use the egui-native open dialog; `project_creator.rs` is currently unused legacy source |
| Mapper auto-detection (LoROM/HiROM/SA-1/ExLoROM/ExHiROM) | ✅ | Detected from header checksum + map-mode byte (`smwe_emu::rom::detect_mapper`): SA-1 packs (Mode $23/$25) map with their base LoROM/HiROM S-CPU bus layout, ExLoROM/ExHiROM get their upper 4 MiB windows; `smwe_rom::MapMode` parses all Mode 2x values and the dev-utils address converter offers ExLoROM/ExHiROM modes |
| Block editor: "acts like" reference | ✅ | `crates/smwe-rom/src/block_behavior.rs` — vanilla dispatches block collision/interaction by hardcoded ID range, not a per-block data byte (source: SMW Central Data Repository, "Detailed explanation of interaction of each tile," MarioFanGamer, 17 Oct 2024). A custom block already gets any of these behaviors for free by using a Map16 ID from the matching range with custom graphics (already fully supported by the existing Map16 editor). Surfaced as an "Acts like: ..." label in `map16_editor.rs` when selecting/editing a block. Ranges + specific tile behaviors covered by tests |
| Block editor: novel (non-vanilla) custom behavior via ASM insertion | ⛔ | Giving a block a behavior that doesn't correspond to any existing vanilla ID range would need a real JSL hook into the interaction dispatcher — genuine new code, not a data patch (unlike the overworld level-number case above). Not started; this is a categorically higher-risk piece of work than anything else in this tracker |
| Graphics editor | ⛔ | README lists as "Planned" |
| ASM code editor | ⛔ | README lists as "Planned" |
| Music editor | ⛔ | README lists as "Planned" |

## Known correctness gaps affecting parity work

- ~~Custom Layer-2 backgrounds in hacked levels can render scrambled~~ FIXED:
  root cause was `UploadSpriteGFX`'s decompression overrunning the `$7EAD00`
  buffer into the `$7EB900` BG tilemap on ROMs with Lunar-Magic-sized GFX files
  (harmless on hardware where the BG is converted to VRAM first; fatal for the
  editor which renders from that WRAM). `decompress_sublevel`/`decompress_extram`
  now snapshot the BG tilemap after `CODE_05801E` and restore it at the end.
- ~~SA-1 and ExLoROM/ExHiROM ROMs are not supported by the mapper or ROM header
  parser~~ FIXED: SA-1 map modes ($23/$25/$33/$35) now parse, SA-1 packs map
  with their base LoROM/HiROM S-CPU bus layout, and ExLoROM/ExHiROM get full
  upper-window address math in both the header parser and the emulator mapper.
- ~~Title/credits editing was U-ROM fixed-address only for the modeled
  slots~~ FIXED (Lunar Magic v1.30 parity): title/credits slot addresses are
  now region-aware, selected from the internal header's region code. Japanese
  ROMs use the J layout (title stripe `$05AF2C`, player-select `$05B358`,
  input sequence `$009BB4`, submap operand `$009663`, enemy-name region
  `$0DF300..$0DFCEB` with the J per-scene sizes) — addresses verified against
  SMWDisX `differences.txt` and the `ver_is_japanese` disassembly branches,
  with the U extraction path re-verified byte-identical against the real ROM
  first. European/other non-J regions keep the U layout (their slots match the
  U addresses). Honest limits: the J Special-World name-update stripes
  (U-only feature) and boss-sequence text (different layout on J ROMs) are
  not modeled; J enemy-name scenes are listed by number because the credits
  text is katakana (no Latin decode implemented).
- Free-space scanning is pure `$FF`-run detection (`find_free_space` /
  `find_free_space_in`): a "run" can start at a live block's `$FF` terminator
  byte (observed on the vanilla ROM — level `0x108`'s Layer-1 terminator at PC
  `0x380EC` begins a 5+ byte `$FF` run that the scanner reports as free). The
  share-data pass guards every erasure with an overlap check against
  still-referenced blocks' measured ranges, so it never erases such a region;
  the level save path's planting does not have that guard.

## Biggest gaps to close for parity (suggested priority)

1. **Block editor: novel custom behavior via ASM insertion** — the "acts like" reference (free, ID-range-based) now works; this remaining piece needs a real JSL hook into the interaction dispatcher, the highest-risk item in this tracker (actual new code, not a data patch).
2. ~~**Message box font/WYSIWYG preview** — raw tile-index byte editing works and is verified against real ROM data; still needed: identify the message font's GFX source (Layer 3 "dynamic stripe image") so users can see/type readable text instead of raw tile numbers.~~ **DONE** via PR #5 (read-only WYSIWYG preview) and PR #6 (editable text with live re-encoding), both merged.
3. **Custom sprite insertion / cluster-extended-generator sprite category editing** — tweaker byte editing now covers the ~0xC9 normal sprite IDs; the other categories still have no dedicated support.
4. ~~**ExGFX colored preview + per-level slot browser cross-linking** — the core import/export loop works now; this is the remaining UX polish.~~ **DONE** — the 8x8 tile editor (`level_editor/tile_editor.rs`, palette-colored grid + pixel painting) delivered the colored preview, and the new "Level GFX Slots" window (`level_editor/gfx_slot_browser.rs`) delivered the slot browser: FG1/FG2/FG3/BG1 resolved via OBJECTGFXLIST and SP1/SP2/SP3/SP4 via the new `SpriteGfxList` ($00A8C3, verified against `bank_00.asm` and the real ROM), each with an Edit button that opens the tile editor on the right GFX file with the level's FG/sprite palette row preselected.
5. ~~**ROM expansion** — free-space *scanning* is now unified (`src/rom_freespace.rs`), but there's still no way to grow the ROM itself (LM's "expand to 3/4MB") for when a hack runs out of space entirely.~~ **DONE** — `crates/smwe-rom/src/rom_expansion.rs` + File > Expand ROM... dialog: expands LoROM images to 1/2/4MB with `$FF` fill and a fixed internal header (size byte + checksum); the new space flows into the unified free-space scanner automatically.
